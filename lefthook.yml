pre-commit:
# ah:managed:start
  ah-check:
    run: ah check
# ah:managed:end

  parallel: true
  commands:
    fmt-check:
      run: cargo fmt -- --check
    check:
      run: cargo check

pre-push:
  commands:
    test:
      run: cargo test
