id = "invoke-help"
description = "Repo-local proof contract for cli/invoke-help."
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo run --bin ah -- --help >/dev/null"
timeout_seconds = 60
