id = "malformed-test-entry-fails-structurally"
description = "Repo-local proof contract for gate/malformed-test-entry-fails-structurally."
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo test -q contracts::tests::"
timeout_seconds = 60
